<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # API ve mevcut dosya/klasörler: dokunma
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteCond %{REQUEST_URI} !^/api/
  RewriteRule ^ index.html [L]
</IfModule>
